import { notFound } from "next/navigation"; import Link from "next/link"; import Navbar from "@/components/Navbar"; import Footer from "@/components/Footer"; import { prisma } from "@/lib/prisma"; import { normalizeRecoveryCode } from "@/lib/recovery-code"; import FoundReportForm from "@/components/recovery/FoundReportForm"; import { ShieldCheck } from "lucide-react";
export default async function RecoveryDetailPage({ params }: { params: Promise<{ recoveryCode: string }> }) { const { recoveryCode } = await params; const item = await prisma.item.findUnique({ where: { recoveryCode: normalizeRecoveryCode(recoveryCode) }, include: { images: { orderBy: { sortOrder: "asc" } }, verifications: { where: { status: "APPROVED" }, select: { id: true } } } }); if (!item) notFound(); return <main><Navbar /><section className="section bg-section recover-detail"><div className="container"><div className="recovery-item-summary"><div><span className="eyebrow">{item.recoveryCode}</span><h1>{item.itemName}</h1><p>{item.description}</p><div className="trust-row"><span>{item.category}</span><span>{item.status}</span>{item.verifications.length ? <span><ShieldCheck size={14} /> Verified item</span> : null}</div>{item.lostModeEnabled && item.rewardAmount ? <p className="reward-note">Reward offered: ₹{item.rewardAmount.toString()} — coordinate safely inside chat.</p> : null}</div><Link className="btn btn-secondary-kp" href="/recover">Search another ID</Link></div><div className="form-section-card"><FoundReportForm recoveryCode={item.recoveryCode ?? ""} /></div></div></section><Footer /></main>; }
