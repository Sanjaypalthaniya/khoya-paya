import { notFound } from "next/navigation"; import AdminDataPage from "@/components/admin/AdminDataPage"; import { getAdmin } from "@/lib/admin"; import { prisma } from "@/lib/prisma";
export default async function Page(){if(!await getAdmin())notFound();const data=await prisma.item.findMany({where:{rewardAmount:{gt:0}},select:{itemName:true,recoveryCode:true,rewardAmount:true,status:true}});return <AdminDataPage title="Rewards." copy="Audit reward notes; payment remains outside the platform." columns={["Item","Recovery ID","Reward","Status"]} rows={data.map(x=>[x.itemName,x.recoveryCode||"—",`₹${x.rewardAmount}`,x.status])}/>;}
