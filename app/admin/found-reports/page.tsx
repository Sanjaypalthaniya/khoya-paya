import { notFound } from "next/navigation"; import AdminDataPage from "@/components/admin/AdminDataPage"; import { getAdmin } from "@/lib/admin"; import { prisma } from "@/lib/prisma";
export default async function Page(){if(!await getAdmin())notFound();const data=await prisma.foundReport.findMany({include:{_count:{select:{matches:true}}},orderBy:{createdAt:"desc"}});return <AdminDataPage title="Found reports." copy="Review matching activity and identify abusive submissions." columns={["Category","Location","Status","Matches"]} rows={data.map(x=>[x.category,x.foundLocation,x.status,x._count.matches])}/>;}
