import { notFound } from "next/navigation"; import AdminDataPage from "@/components/admin/AdminDataPage"; import { getAdmin } from "@/lib/admin"; import { prisma } from "@/lib/prisma";
export default async function Page(){if(!await getAdmin())notFound();const data=await prisma.finderIdentity.findMany({orderBy:{reputationScore:"desc"}});return <AdminDataPage title="Finder reputation." copy="Monitor helpful recoveries and false-report signals." columns={["Finder","Score","Recoveries","False reports"]} rows={data.map(x=>[x.name||x.email||"Anonymous",x.reputationScore,x.successfulRecoveries,x.falseReports])}/>;}
