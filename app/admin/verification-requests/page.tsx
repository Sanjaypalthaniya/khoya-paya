import { notFound } from "next/navigation"; import AdminDataPage from "@/components/admin/AdminDataPage"; import { getAdmin } from "@/lib/admin"; import { prisma } from "@/lib/prisma";
export default async function Page(){if(!await getAdmin())notFound();const data=await prisma.userVerificationRequest.findMany({include:{user:true},orderBy:{createdAt:"desc"}});return <AdminDataPage title="Verification requests." copy="Approve trust signals without exposing proof documents in public surfaces." columns={["User","Type","Status"]} rows={data.map(x=>[x.user.email,x.type,x.status])}/>;}
