import { notFound } from "next/navigation"; import AdminDataPage from "@/components/admin/AdminDataPage"; import { getAdmin } from "@/lib/admin"; import { prisma } from "@/lib/prisma";
export default async function Page(){if(!await getAdmin())notFound();const values=await Promise.all([prisma.item.count({where:{status:"LOST"}}),prisma.recoveryRequest.count({where:{status:"COMPLETED"}}),prisma.foundReport.count({where:{status:"MATCHED"}}),prisma.conversation.count({where:{status:"OPEN"}})]);return <AdminDataPage title="Recovery analytics." copy="Operational health across the recovery ecosystem." columns={["Metric","Value"]} rows={[["Lost items",values[0]],["Completed recoveries",values[1]],["Matched reports",values[2]],["Open chats",values[3]]]}/>;}
