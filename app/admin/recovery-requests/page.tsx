import { notFound } from "next/navigation"; import AdminDataPage from "@/components/admin/AdminDataPage"; import { getAdmin } from "@/lib/admin"; import { prisma } from "@/lib/prisma";
export default async function Page(){if(!await getAdmin())notFound();const data=await prisma.recoveryRequest.findMany({include:{item:true,owner:true},orderBy:{updatedAt:"desc"}});return <AdminDataPage title="Recovery requests." copy="Audit each recovery workflow and its current status." columns={["Item","Owner","Source","Status"]} rows={data.map(x=>[x.item.itemName,x.owner.email,x.source,x.status])}/>;}
