import { notFound } from "next/navigation"; import AdminDataPage from "@/components/admin/AdminDataPage"; import { getAdmin } from "@/lib/admin"; import { prisma } from "@/lib/prisma";
export default async function Page(){if(!await getAdmin())notFound();const data=await prisma.conversation.findMany({include:{item:true,_count:{select:{messages:true}}},orderBy:{updatedAt:"desc"}});return <AdminDataPage title="Secure chats." copy="Read-only metadata view for abuse and recovery operations." columns={["Item","Finder","Status","Messages"]} rows={data.map(x=>[x.item.itemName,x.finderName||"Anonymous",x.status,x._count.messages])}/>;}
