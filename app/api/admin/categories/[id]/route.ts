import { z } from "zod"; import { getAdmin } from "@/lib/admin"; import { prisma } from "@/lib/prisma"; import { errorResponse, successResponse, unauthorizedResponse, validationErrorResponse } from "@/lib/api-response";
const schema = z.object({ name: z.string().trim().min(2).max(80).optional(), slug: z.string().trim().regex(/^[a-z0-9-]+$/).max(90).optional(), icon: z.string().trim().max(80).nullable().optional(), isActive: z.boolean().optional() });
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!await getAdmin()) return unauthorizedResponse(); const parsed = schema.safeParse(await request.json().catch(() => null)); if (!parsed.success) return validationErrorResponse(parsed.error); const { id } = await params; const category = await prisma.category.update({ where: { id }, data: parsed.data }).catch(() => null); return category ? successResponse("Category updated.", { category }) : errorResponse("Category not found.", 404); }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { if (!await getAdmin()) return unauthorizedResponse(); const { id } = await params; const category = await prisma.category.update({ where: { id }, data: { isActive: false } }).catch(() => null); return category ? successResponse("Category archived.") : errorResponse("Category not found.", 404); }
