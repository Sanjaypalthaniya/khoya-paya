import { communityFailure,communitySuccess } from "@/lib/community/api";import { requireCommunityUser,enforceCommunityRateLimit } from "@/lib/community/route-auth";import { generateHandoverCode } from "@/lib/claims/service";export async function POST(request:Request,{params}:{params:Promise<{claimId:string}>}){try{const user=await requireCommunityUser();enforceCommunityRateLimit(request,"handover-generate",user.id,5,60*60_000);const code=await generateHandoverCode((await params).claimId,user.id);return communitySuccess("One-time handover code generated. It expires in 15 minutes.",{code,expiresInSeconds:900})}catch(error){return communityFailure(error,"claim.handover.generate")}}
