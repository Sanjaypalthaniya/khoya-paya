import { prisma } from "@/lib/prisma"; import { errorResponse, successResponse } from "@/lib/api-response";
export async function GET(_: Request, { params }: { params: Promise<{ token: string }> }) { const { token } = await params; const conversation = await prisma.conversation.findUnique({ where: { finderAccessToken: token }, select: { id: true } }); if (!conversation) return errorResponse("Secure chat link is invalid.", 404); const questions = await prisma.verificationQuestion.findMany({ where: { conversationId: conversation.id, status: { in: ["SENT", "ANSWERED", "VERIFIED", "REJECTED"] } }, select: { id: true, question: true, answer: true, status: true }, orderBy: { createdAt: "asc" } }); return successResponse("Questions loaded.", { questions }); }
