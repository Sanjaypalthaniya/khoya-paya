import { NextResponse } from "next/server";import { togglePostReaction } from "@/lib/community/interactions";import { reactionSchema } from "@/lib/validations/community-interactions";import { requireCommunityUser,enforceCommunityRateLimit } from "@/lib/community/route-auth";import { communityFailure,communitySuccess } from "@/lib/community/api";
export async function PUT(req:Request,{params}:{params:Promise<{postId:string}>}){try{const u=await requireCommunityUser();enforceCommunityRateLimit(req,"reaction",u.id,30);const {postId}=await params;const body=reactionSchema.parse(await req.json());return communitySuccess("Reaction updated",await togglePostReaction(u.id,postId,body.reactionType))}catch(e){return communityFailure(e,"community.reaction")}}
export async function DELETE(req:Request,{params}:{params:Promise<{postId:string}>}){try{const u=await requireCommunityUser();const {postId}=await params;return communitySuccess("Reaction removed",await togglePostReaction(u.id,postId,"LIKE"))}catch(e){return communityFailure(e,"community.reaction.delete")}}
