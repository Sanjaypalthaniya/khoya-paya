import { communityFailure,communitySuccess } from "@/lib/community/api";import { requireCommunityUser,enforceCommunityRateLimit } from "@/lib/community/route-auth";import { publishCommunityPost } from "@/lib/community/service";type Context={params:Promise<{postId:string}>};
export async function POST(request:Request,{params}:Context){try{const user=await requireCommunityUser();enforceCommunityRateLimit(request,"publish",user.id,10);const post=await publishCommunityPost(user.id,(await params).postId);return communitySuccess(post.moderation.status==="APPROVED"?"Post published.":post.moderation.reason??"Post is under review.",post)}catch(error){return communityFailure(error,"community-publish-post")}}
