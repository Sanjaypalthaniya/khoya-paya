import { getCurrentUser } from "@/lib/auth";import { communityFailure,communitySuccess } from "@/lib/community/api";import { CommunityError } from "@/lib/community/errors";import { prisma } from "@/lib/prisma";
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{const user=await getCurrentUser();if(!user)throw new CommunityError("UNAUTHENTICATED","Authentication is required.",401);const deleted=await prisma.searchHistory.deleteMany({where:{id:(await params).id,userId:user.id}});if(!deleted.count)throw new CommunityError("NOT_FOUND","Search entry not found.",404);return communitySuccess("Search entry removed",{})}catch(error){return communityFailure(error,"community.search.history.delete")}}
