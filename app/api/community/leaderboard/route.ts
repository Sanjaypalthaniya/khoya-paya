import { LeaderboardPeriod,LeaderboardType } from "@prisma/client";import { z } from "zod";import { communityFailure,communitySuccess } from "@/lib/community/api";import { leaderboard } from "@/lib/community/leaderboard";
const schema=z.object({type:z.nativeEnum(LeaderboardType).default("TOP_HELPERS"),period:z.nativeEnum(LeaderboardPeriod).default("WEEKLY"),limit:z.coerce.number().int().min(1).max(50).default(10)});export async function GET(req:Request){try{const input=schema.parse(Object.fromEntries(new URL(req.url).searchParams));return communitySuccess("Leaderboard snapshot",await leaderboard(input.type,input.period,input.limit))}catch(error){return communityFailure(error,"community.leaderboard")}}
