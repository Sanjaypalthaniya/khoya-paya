import { requireCommunityUser } from "@/lib/community/route-auth";import { communityFailure,communitySuccess } from "@/lib/community/api";import { periodKey } from "@/lib/community/leaderboard";import { prisma } from "@/lib/prisma";
export async function GET(){try{const user=await requireCommunityUser(),entry=await prisma.leaderboardSnapshot.findFirst({where:{userId:user.id,leaderboardType:"TOP_HELPERS",periodType:"WEEKLY",periodKey:periodKey("WEEKLY"),locationScope:"GLOBAL"},select:{rank:true,score:true,periodKey:true,calculatedAt:true}});return communitySuccess("Your leaderboard position",entry)}catch(error){return communityFailure(error,"community.leaderboard.me")}}
