import { getCurrentUser } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { successResponse, unauthorizedResponse } from "@/lib/api-response";
export async function GET() { const user = await getCurrentUser(); if (!user || user.isBlocked) return unauthorizedResponse(); const matches = await prisma.foundReportMatch.findMany({ where: { item: { userId: user.id } }, include: { item: { select: { itemName: true } }, foundReport: { select: { id: true, category: true, brand: true, color: true, description: true, foundLocation: true, foundDate: true, status: true, createdAt: true } } }, orderBy: { createdAt: "desc" } }); return successResponse("Found reports loaded.", { matches }); }
