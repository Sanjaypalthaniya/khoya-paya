import { getCurrentUser } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { successResponse, unauthorizedResponse } from "@/lib/api-response";
export async function GET() { const user = await getCurrentUser(); if (!user || user.isBlocked) return unauthorizedResponse(); const requests = await prisma.recoveryRequest.findMany({ where: { ownerId: user.id }, include: { item: { select: { itemName: true, recoveryCode: true } }, timeline: { orderBy: { createdAt: "asc" } }, foundReport: { select: { foundLocation: true, foundDate: true } } }, orderBy: { updatedAt: "desc" } }); return successResponse("Recovery requests loaded.", { requests }); }
