import { redirect } from "next/navigation";
import DashboardLayout from "@/components/dashboard/DashboardLayout";
import { getCurrentUser } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
export default async function MyReportsPage(){const user=await getCurrentUser();if(!user)redirect("/login");const reports=await prisma.communityReport.findMany({where:{reporterId:user.id},orderBy:{createdAt:"desc"},take:50,select:{id:true,targetType:true,reason:true,status:true,resolution:true,createdAt:true,targetPost:{select:{title:true}},targetComment:{select:{content:true}},targetUser:{select:{name:true}}}});return <main className="container py-5"><DashboardLayout active="/dashboard/my-reports"><h1>My reports</h1><p>Only you can see these confidential report references and safe status updates.</p><div className="table-responsive"><table className="table"><thead><tr><th>Reference</th><th>Target</th><th>Reason</th><th>Submitted</th><th>Status</th><th>Resolution</th></tr></thead><tbody>{reports.map(report=><tr key={report.id}><td>{report.id}</td><td>{report.targetPost?.title??report.targetComment?.content.slice(0,80)??report.targetUser?.name??"Removed content"}</td><td>{report.reason.replaceAll("_"," ")}</td><td>{report.createdAt.toLocaleDateString()}</td><td>{report.status}</td><td>{report.resolution??"Pending review"}</td></tr>)}</tbody></table></div></DashboardLayout></main>}
