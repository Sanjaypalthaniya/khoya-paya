import DashboardLayout from "@/components/dashboard/DashboardLayout"; import Navbar from "@/components/Navbar"; import Footer from "@/components/Footer"; import { getCurrentUser } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { redirect } from "next/navigation"; import { formatDateTime } from "@/lib/date-time";
export default async function FoundReportsPage() { const user = await getCurrentUser(); if (!user) redirect("/login"); const matches = await prisma.foundReportMatch.findMany({ where: { item: { userId: user.id } }, include: { item: true, foundReport: true }, orderBy: { createdAt: "desc" } }); return <main><Navbar /><section className="section bg-section"><div className="container"><DashboardLayout active="/dashboard/found-reports"><div className="conversation-list">{matches.length ? matches.map((match) => <article className="conversation-card" key={match.id}><div className="conversation-copy"><div><h3>{match.foundReport.category} near {match.foundReport.foundLocation}</h3><span className="status-pill">{match.matchScore}% match</span></div><small>Possible match for {match.item.itemName}</small><p>{match.foundReport.description}</p><time>{formatDateTime(match.foundReport.foundDate)}</time></div></article>) : <div className="empty-state"><h3>No possible matches</h3><p>Smart matching reports will appear here.</p></div>}</div></DashboardLayout></div></section><Footer /></main>; }
