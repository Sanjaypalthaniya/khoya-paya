import DashboardLayout from "@/components/dashboard/DashboardLayout"; import Navbar from "@/components/Navbar"; import Footer from "@/components/Footer"; import { getCurrentUser } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { redirect } from "next/navigation"; import { formatDateTime } from "@/lib/date-time";
export default async function RecoveryRequestsPage() { const user = await getCurrentUser(); if (!user) redirect("/login"); const requests = await prisma.recoveryRequest.findMany({ where: { ownerId: user.id }, include: { item: true, timeline: { orderBy: { createdAt: "asc" } } }, orderBy: { updatedAt: "desc" } }); return <main><Navbar /><section className="section bg-section"><div className="container"><DashboardLayout active="/dashboard/recovery-requests"><div className="recovery-request-list">{requests.length ? requests.map((request) => <article className="dashboard-message-card" key={request.id}><div className="dash-toolbar"><div><small>{request.source}</small><h3>{request.item.itemName}</h3></div><span className="status-pill">{request.status}</span></div><div className="recovery-timeline">{request.timeline.map((event) => <div key={event.id}><i /><div><strong>{event.title}</strong><p>{event.description}</p><small>{formatDateTime(event.createdAt)}</small></div></div>)}</div></article>) : <div className="empty-state"><h3>No recovery requests yet</h3><p>Recovery workflows will appear when a finder reports a possible match.</p></div>}</div></DashboardLayout></div></section><Footer /></main>; }
