import Link from "next/link"; import Navbar from "@/components/Navbar"; import Footer from "@/components/Footer"; import PageHero from "@/components/PageHero"; import { prisma } from "@/lib/prisma";
export default async function LostItemsPage({ searchParams }: { searchParams: Promise<{ q?: string; category?: string }> }) { const { q = "", category } = await searchParams; const items = await prisma.item.findMany({ where: { status: "LOST", publicSearchVisible: true, ...(category ? { category } : {}), ...(q ? { OR: [{ itemName: { contains: q, mode: "insensitive" } }, { description: { contains: q, mode: "insensitive" } }, { lastSeenLocation: { contains: q, mode: "insensitive" } }] } : {}) }, take: 30, orderBy: { updatedAt: "desc" } }); return <main><Navbar /><PageHero eyebrow="Community recovery" title="Public lost items." copy="Search privacy-safe listings and use the Recovery ID to notify an owner securely." /><section className="section bg-section"><div className="container"><form className="recovery-search-card"><label>Search<input name="q" defaultValue={q} placeholder="Category, color, location…" /></label><button className="btn btn-primary-kp">Search</button></form><div className="row g-4 mt-2">{items.map((item) => <div className="col-md-4" key={item.id}><article className="dashboard-message-card h-100"><span className="status-pill">{item.category}</span><h3>{item.itemName}</h3><p>{item.description}</p><small>Last seen: {item.lastSeenLocation || "Not shared"}</small><Link className="btn btn-secondary-kp mt-3" href={`/recover/${item.recoveryCode}`}>I found this item</Link></article></div>)}</div></div></section><Footer /></main>; }
