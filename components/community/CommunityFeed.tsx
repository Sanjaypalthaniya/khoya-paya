"use client";
import { useCallback,useEffect,useState } from "react";import { AlertTriangle,LoaderCircle,RefreshCw,SearchX,WifiOff } from "lucide-react";import type { CommunityPost,FeedState } from "@/lib/community/types";import CommunityPostCard from "./CommunityPostCard";import CreatePostComposer from "./CreatePostComposer";
const filters=["All","Lost","Found","Latest","Verified","Reward","Success Stories","Missing Pets","Documents","Vehicles"];
type ApiPost={id:string;postType:CommunityPost["type"];itemCategory:string;title:string;description:string;status:string;visibility:"PUBLIC"|"PRIVATE"|"UNLISTED";publicLocation:{name:string|null;city:string|null;state:string|null};reward:{amount:string|null;currency:string}|null;eventDate:string|null;media:CommunityPost["media"];tags:Array<{displayName:string}>;author:{id:string;displayName:string;initials:string;verified:boolean};counters:{reactions:number;comments:number;views:number};createdAt:string;publishedAt:string|null};
function mapPost(post:ApiPost):CommunityPost{return{id:post.id,type:post.postType,status:post.status==="RECOVERED"?"RECOVERED":post.postType.startsWith("FOUND")?"FOUND":"LOST",author:{id:post.author.id,displayName:post.author.displayName,username:post.author.id,initials:post.author.initials,verified:post.author.verified},title:post.title,description:post.description,location:[post.publicLocation.name,post.publicLocation.city,post.publicLocation.state].filter(Boolean).join(", "),eventDate:post.eventDate??undefined,createdAt:new Date(post.publishedAt||post.createdAt).toLocaleString(),visibility:post.visibility==="PUBLIC"?"PUBLIC":"COMMUNITY",category:post.itemCategory,reward:post.reward?.amount?`${post.reward.currency} ${post.reward.amount}`:undefined,tags:post.tags.map(t=>t.displayName),media:post.media,reactions:{LIKE:post.counters.reactions},commentCount:post.counters.comments,viewCount:post.counters.views}}
function queryFor(filter:string,cursor?:string|null){const params=new URLSearchParams({limit:"6"});if(cursor)params.set("cursor",cursor);if(filter==="Lost")params.set("type","LOST_ITEM");if(filter==="Found")params.set("type","FOUND_ITEM");if(filter==="Verified")params.set("verifiedOnly","true");if(filter==="Reward")params.set("rewardOnly","true");if(filter==="Success Stories")params.set("type","SUCCESS_STORY");if(filter==="Missing Pets")params.set("type","MISSING_PET");if(filter==="Documents")params.set("category","DOCUMENTS");if(filter==="Vehicles")params.set("category","VEHICLE");return params}
export default function CommunityFeed({initials="U"}:{initials?:string}){const[state,setState]=useState<FeedState>("INITIAL_LOADING");const[filter,setFilter]=useState("All");const[posts,setPosts]=useState<CommunityPost[]>([]);const[nextCursor,setNextCursor]=useState<string|null>(null);const[hasMore,setHasMore]=useState(false);const[error,setError]=useState("");const load=useCallback(async(reset:boolean,selected:string,cursor:string|null=null)=>{setState(reset?"REFRESHING":"LOADED");setError("");try{const response=await fetch(`/api/community/feed?${queryFor(selected,reset?null:cursor)}`,{cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error(body.message||"Unable to load the feed.");const incoming=(body.data.items as ApiPost[]).map(mapPost);setPosts(current=>reset?incoming:[...current,...incoming]);setNextCursor(body.data.nextCursor);setHasMore(body.data.hasMore);setState(incoming.length||!reset?body.data.hasMore?"LOADED":"END":"EMPTY")}catch(cause){setError(cause instanceof Error?cause.message:"Unable to load the feed.");setState(navigator.onLine?"ERROR":"OFFLINE")}},[]);useEffect(()=>{const task=window.setTimeout(()=>void load(true,filter),0);return()=>window.clearTimeout(task)},[filter,load]);
 return <div className="community-feed-v2"><CreatePostComposer initials={initials} onSaved={()=>load(true,filter)}/><div className="feed-filter-shell"><div className="feed-filters" role="tablist" aria-label="Community feed filters">{filters.map(item=><button role="tab" aria-selected={filter===item} className={filter===item?"active":""} type="button" key={item} onClick={()=>{setFilter(item);setState("FILTERED")}}>{item}</button>)}</div></div><div className="feed-intro"><div><span>Community feed</span><h2>{filter==="All"?"Latest around you":filter}</h2></div><button type="button" onClick={()=>load(true,filter)}><RefreshCw size={15}/>Refresh</button></div>{(state==="INITIAL_LOADING"||state==="REFRESHING")&&<div className="feed-skeleton" aria-label="Loading community posts"><div/><div/><div/><LoaderCircle className="spin"/></div>}{state==="ERROR"&&<div className="feed-state"><AlertTriangle/><strong>{error}</strong><button onClick={()=>load(true,filter)}>Retry</button></div>}{state==="OFFLINE"&&<div className="feed-state"><WifiOff/><strong>You&apos;re offline</strong><button onClick={()=>load(true,filter)}>Retry</button></div>}{state==="EMPTY"&&<div className="feed-state"><SearchX/><strong>No posts match this filter</strong><span>Publish the first privacy-safe community post.</span></div>}{!["INITIAL_LOADING","REFRESHING","ERROR","OFFLINE"].includes(state)&&posts.map(post=><CommunityPostCard post={post} key={post.id}/>)}{hasMore?<button className="load-more" type="button" onClick={()=>load(false,filter,nextCursor)}>Load more posts</button>:posts.length>0&&<div className="community-card end-feed"><strong>You&apos;re all caught up</strong><p>New community posts will appear here.</p></div>}</div>}
