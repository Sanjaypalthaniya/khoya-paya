"use client";
import { useMemo, useState } from "react";
import Link from "next/link";
import { ChevronDown, Search, SearchX } from "lucide-react";
export type FaqItem={id:string;category:string;question:string;answer:string};
export default function FaqExplorer({items}:{items:FaqItem[]}){const[query,setQuery]=useState("");const[category,setCategory]=useState("All");const categories=["All",...Array.from(new Set(items.map(i=>i.category)))];const filtered=useMemo(()=>items.filter(i=>(category==="All"||i.category===category)&&`${i.question} ${i.answer}`.toLowerCase().includes(query.toLowerCase())),[category,items,query]);return <div className="faq-explorer"><div className="faq-search"><Search size={19}/><label className="visually-hidden" htmlFor="faq-search">Search frequently asked questions</label><input id="faq-search" type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search questions and answers"/></div><div className="faq-categories" role="tablist" aria-label="FAQ categories">{categories.map(item=><button type="button" role="tab" aria-selected={category===item} className={category===item?"active":""} onClick={()=>setCategory(item)} key={item}>{item}</button>)}</div><div className="faq-results" aria-live="polite">{filtered.map((item,index)=><details id={item.id} key={item.id} open={index===0}><summary>{item.question}<ChevronDown size={19}/></summary><p>{item.answer}</p><Link href={`#${item.id}`} aria-label={`Copyable link to ${item.question}`}>Link to this answer</Link></details>)}{!filtered.length&&<div className="marketing-empty"><SearchX/><h2>No matching questions</h2><p>Try another category or a shorter search phrase.</p><Link href="/contact">Contact support</Link></div>}</div></div>}
