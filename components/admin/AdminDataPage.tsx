import Link from "next/link"; import Navbar from "@/components/Navbar"; import Footer from "@/components/Footer"; import PageHero from "@/components/PageHero";
export default function AdminDataPage({ title, copy, columns, rows }: { title: string; copy: string; columns: string[]; rows: Array<Array<string | number>> }) { return <main><Navbar /><PageHero eyebrow="Admin" title={title} copy={copy} /><section className="section bg-section"><div className="container"><div className="message-actions mb-3"><Link href="/admin" className="btn btn-secondary-kp btn-sm-pill">Admin overview</Link></div><article className="dashboard-message-card"><div className="scan-table-wrap"><table className="scan-table"><thead><tr>{columns.map((column) => <th key={column}>{column}</th>)}</tr></thead><tbody>{rows.length ? rows.map((row, index) => <tr key={index}>{row.map((cell, cellIndex) => <td key={cellIndex}>{cell}</td>)}</tr>) : <tr><td colSpan={columns.length}>No records found.</td></tr>}</tbody></table></div></article></div></section><Footer /></main>; }
